id: domain:durable-agent-workflows
kind: domain
title: Durable Agent Work Slices
summary: LLM-agent-specific work-slice contracts, approval, leases, recovery, review evidence, and outcome archive behavior that must not become a generic workflow runtime.
visibility: public
relations:
  - type: contains
    target: workflow:resume-workloop
  - type: contains
    target: workflow:adjudicate-workloop-slice
  - type: contains
    target: component:workloop-schema
