<!-- Generated by Agent Atlas. Do not edit directly. -->

# Verification

Profile: `public`

## Test Scopes

- `test-scope:adjudication-tests` - Adjudication Tests: Unit tests for outcome and peer-review adjudication.
- `test-scope:ai-quality-loops-adapter-tests` - AI Quality Loops Adapter Tests: Verifies WorkLoop slice review context maps to an injected ai-quality-loops-compatible runner.
- `test-scope:generated.package.agent-workloops-api` - @agent-workloops/api tests: 1 discovered test file under packages/api.
- `test-scope:generated.package.agent-workloops-cli` - @agent-workloops/cli tests: 1 discovered test file under packages/cli.
- `test-scope:generated.package.agent-workloops-server` - @agent-workloops/server tests: 2 discovered test files under packages/server.
- `test-scope:generated.package.agent-workloops-web` - @agent-workloops/web tests: 3 discovered test files under apps/web.
- `test-scope:generated.src` - src tests: 4 discovered test files under src.
- `test-scope:hosted-workloops-tests` - Hosted Workloops Tests: Package tests for hosted API contracts, server approval and lease behavior, CLI parsing, and web queue bucketing.
- `test-scope:launcher-tests` - Launcher Tests: Verifies Codex launch prompt rendering and launch-record generation for WorkLoop slices.
- `test-scope:package-check` - Package Check: Full package verification including typecheck, unit tests, and Atlas maintenance checks.
- `test-scope:selection-tests` - Selection Tests: Unit tests for slice selection and running-state transitions.

## Adjudication Tests

ID: `test-scope:adjudication-tests`

Unit tests for outcome and peer-review adjudication.

- command: `pnpm test -- src/adjudication.test.ts`
  purpose: Verify WorkLoop decision routing and state application.

Verifies:
- `component:workloop-adjudication`
- `workflow:adjudicate-workloop-slice`

## AI Quality Loops Adapter Tests

ID: `test-scope:ai-quality-loops-adapter-tests`

Verifies WorkLoop slice review context maps to an injected ai-quality-loops-compatible runner.

Verifies:
- `component:ai-quality-loops-adapter`

## @agent-workloops/api tests

ID: `test-scope:generated.package.agent-workloops-api`

1 discovered test file under packages/api.

- command: `pnpm test`
  purpose: Run repository test suite.

## @agent-workloops/cli tests

ID: `test-scope:generated.package.agent-workloops-cli`

1 discovered test file under packages/cli.

- command: `pnpm test`
  purpose: Run repository test suite.

## @agent-workloops/server tests

ID: `test-scope:generated.package.agent-workloops-server`

2 discovered test files under packages/server.

- command: `pnpm test`
  purpose: Run repository test suite.

## @agent-workloops/web tests

ID: `test-scope:generated.package.agent-workloops-web`

3 discovered test files under apps/web.

- command: `pnpm test`
  purpose: Run repository test suite.

## src tests

ID: `test-scope:generated.src`

4 discovered test files under src.

- command: `pnpm test`
  purpose: Run repository test suite.

## Hosted Workloops Tests

ID: `test-scope:hosted-workloops-tests`

Package tests for hosted API contracts, server approval and lease behavior, CLI parsing, and web queue bucketing.

- command: `pnpm --filter @agent-workloops/api test`
  purpose: Verify hosted API contract schemas.

- command: `pnpm --filter @agent-workloops/server test`
  purpose: Verify server approval, token, lease, and archive behavior.

- command: `pnpm --filter @agent-workloops/cli test`
  purpose: Verify CLI parsing and idle executor behavior.

- command: `pnpm --filter @agent-workloops/web test`
  purpose: Verify web queue bucketing behavior.

Verifies:
- `component:hosted-workloops-api`
- `component:hosted-workloops-cli`
- `component:hosted-workloops-server`
- `component:hosted-workloops-web`

## Launcher Tests

ID: `test-scope:launcher-tests`

Verifies Codex launch prompt rendering and launch-record generation for WorkLoop slices.

Verifies:
- `component:workloop-codex-launcher`

## Package Check

ID: `test-scope:package-check`

Full package verification including typecheck, unit tests, and Atlas maintenance checks.

- command: `pnpm check`
  purpose: Verify TypeScript, tests, and Atlas drift.

Verifies:
- `repository:agent-workloops`

## Selection Tests

ID: `test-scope:selection-tests`

Unit tests for slice selection and running-state transitions.

- command: `pnpm test -- src/selection.test.ts`
  purpose: Verify WorkLoop slice selection behavior.

Verifies:
- `component:workloop-selection`
- `workflow:resume-workloop`
