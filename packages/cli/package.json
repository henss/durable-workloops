{
  "name": "@agent-workloops/cli",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "bin": {
    "agent-workloops": "./dist/index.js"
  },
  "scripts": {
    "build": "pnpm --filter agent-workloops build && pnpm --filter @agent-workloops/api build && tsc -p tsconfig.build.json",
    "typecheck": "pnpm --filter agent-workloops build && pnpm --filter @agent-workloops/api build && tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@agent-workloops/api": "workspace:*",
    "agent-workloops": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}
